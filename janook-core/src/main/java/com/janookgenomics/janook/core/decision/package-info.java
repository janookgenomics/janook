/**
 * The decision rules of Table 6: what turns a weight tally into a label.
 *
 * Two branches that always both run, then a joining step. The rules count the tally's groups;
 * nothing here reads a criterion's code or its definition.
 */
package com.janookgenomics.janook.core.decision;
