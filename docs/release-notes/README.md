# Release notes

One file per release tag, named exactly as the tag: `v9.0.0.md`. The release workflow refuses to
publish a tag whose notes file does not exist, and appends the artifact checksum to it.

Notes say what the release contains and what it deliberately omits, in plain language. They are
written by a person before the tag, not generated after it.
